import 'package:flutter/material.dart';
import 'package:tinnierenee12/widget/appbar/custom_appbar.dart';

class ClientTransactionHistryScreen extends StatelessWidget {
  const ClientTransactionHistryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: CustomAppbar(title: 'Transaction History'),
    
    body: Column(
      children: [
        
      ],
    ),
    );
  }
}
